# Types

Shared TypeScript contracts for API responses, frontend DTOs, and future cross-package domain types.

Keep this package small. Add types only when they are shared across app or package boundaries.
